//Chris Estrada
//Date: 9/25/26
//Assignment Name- Challenge #5 - Pet Registry API
//What you did-Built an API for staff and pets at an adoption place. built a controller, service, i service, and data file for each along with a database to house both lists. Users have the ability to get all pets, get by id, add a pet, update the pets information, adopt a pet and remove the pet from the database. The user can also view staff and update their job and salary. the delete is a soft delete that updates the table to show "isDeleted" as false and "gatallpets" will only show pets where isDeleted is false, so this effectively removes them but still leaves the record in case the pet is added back.
//Peer Review: Valery Lot
//Review: The Update function in the Pets section wouldn't save to the database although I was getting the 204 No Content message. I'm unable to get the Patch to work for the staff. Also, missing the function to see which staff is working. Otherwise, everything else works good. 